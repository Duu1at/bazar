import 'package:flutter/material.dart';

class SubmitView extends StatelessWidget {
  const SubmitView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('SubmitView'));
  }
}
